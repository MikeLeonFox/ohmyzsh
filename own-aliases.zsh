# ~/.oh-my-zsh/custom/own-aliases.zsh
# Main loader for modular aliases
# This file now sources all the modular alias files from the init.zsh

# Source the main initialization file
[[ -f "$ZSH_CUSTOM/init.zsh" ]] && source "$ZSH_CUSTOM/init.zsh"
